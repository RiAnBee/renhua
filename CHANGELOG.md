# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 调研结论：韵律线立项试做后**关闭**（不产出可用机制）

用户授权试做「韵律/庄雅度」这条线 —— 文献调研把它列为理论上唯一**非词表机制**
（汉语有音节组配约束，某些组配天生「不上口」，与用词无关）。**结论是不能。**
完整记录见 `evidence.md` §十一，复现脚本 `scripts/prosody_features.py`。

| 测量 | 结果 | 有效性 |
|---|---|---|
| **音节组配四格分布**（不需词表） | 两语域**全部无差异**（0.93–1.13×，无稳健项） | ✅ 有效 → **无信号** |
| 述宾/定中结构内组配（需 POS） | 唯一信号：述宾[2+1] 口语 1.74×，**正式 1.03× n.s.** | ❌ **测量无效** |
| 合偶词配单音节（封闭小类） | AI 正式仅 3 例，样本不足 | ⚠️ 无法判定 |

**核心结论**：**AI 中文完整复现了人类中文的音节组配分布** —— 四格组配的两语域倍率
全在 0.93–1.13× 之间。

**为什么结构内组配的测量无效**（已写进脚本顶部，防止重做）：`jieba.posseg` 的
「动词+名词」邻接会把 **VN+N 定中复合词**误判为述宾。实测高频误判项
「提示词」「优化器」「工作流」「传承人」「测报灯」**全是名词**。它们按话题词表分布，
所以初次测出的「述宾[2+1] 口语 1.77×」实际是**话题差异**，不是韵律违规。
可靠的述宾识别需要依存句法分析器，本项目没有。

**另有两处拿不到**：冯胜利 2005「文白比例」需**人工判定**哪句「较文」
（王力《小气》12 逗中 5 处 → 40%）；2008《庄雅度自动测量》的算式需 350–500 条
嵌偶词/合偶词**词表**（论文付费，OA 版附录只摘 4 对【书】/【白】对照）。

**即使有信号也不适用**：韵律修正的动作是**替换**（「*种植树」→「种树」/「种植树木」），
需要知道替代说法，超出本 skill 的「删/合并/移/还原」动作集。

**行数预算同步放宽**：350 → **360**（`CONTRIBUTING.md`）。A4b 拆条后 A4 需容纳两条
子规则，350 行会把规则压到读不懂。放宽的是总行数，不是信息密度。

### 新增：A4b 并列补充连词（两语域通用），A4 拆为 a/b；A5 补中文修辞学依据

**背景**：回归中文学界本身做了一轮文献调研（六路 + 第二轮六路补漏），**推翻了 `evidence.md`
第五节「中文句法现象级证据 = 0 篇」的旧断言** —— 当时只搜英文文献与 NLP 会议，漏掉了中文学界
自己的期刊。实际存在多篇直接的中文机对比研究。详见 `evidence.md` §十。

**改动一：A4 拆成 A4a / A4b**

原 A4 只有一条规则（转折连词），且因语域依赖被限制在正式语域。拆开连词子类后发现
**转折类与并列补充类的语域行为不同**：

| 子类 | 正式 | 口语 | 结论 |
|---|---|---|---|
| 转折 | 6.43×（p<0.001） | 0.08×（p<0.01） | 方向相反 → 仍仅正式语域 |
| **并列补充** | **5.72×（p<0.001）** | **3.33×（p<0.01）** | **两语域同向 → 两类语域都启用** |

→ A4b 是 A4 里第一个**两语域通用**的子条，把一条被语域困住的规则救回了一半。

**关键定义决定**：`以及` **不计入**。它是短语内并列词（"A、B 以及 C"），不是句际衔接标记；
计入会抬高人类侧、把倍率从 5.72× 稀释到 2.63×。A4b 只取**句首**形态。

**独立印证**：Kong & Macken 2025（MT Summit, arXiv:2506.22050）用**人民日报+新华网**
原生中文对比 11 个翻译引擎，其 top-10 判别特征含 `ratio_paraConj`（并列连词）与
`ratio_advrstvConj`（转折连词）—— 与本项目 A4a/A4b 两条**独立地**吻合。该文语料与本项目
人类正式语料**同源**。

**改动二：A5 补中文修辞学依据**

`证据.md` A5 原只有 P 级（从业者博客）。中文修辞学有**同构的经典表述**（王希杰《汉语修辞学》
风格原则：「只有适度才能不让……繁丰变成冗杂，简洁变成干枯」），与「删到留下文眼」是同一判断。
同时新增一条**必须避开的误配**：陈望道的「积极修辞／消极修辞」**不等于**「繁复／简洁」或
「好／坏」；消极修辞是正面标准，不是本动作集。

**同时修正**：A3 里「段末句长 CV 只在正式语域显著」是 A5 的证据，误挂在 A3，已移除（去重）。

**验证**：CONTRIBUTING 要求的三项负向测试全部通过 ——
① 人类自写随笔 → **0 处改动**（A3 命中但被反例门挡下）
② 公文通知 → **0 处改动**（闸门 1 生效）
③ AI 稿（含 3 处段首「同时/此外」）→ A4b 命中 3 处、A1 命中 1 处，改动全部可指到规则号
另加 A4b 检测契约 9 例（含「以及」反例）全部通过。

**行数**：SKILL.md 新增规则的同时压缩了 A4/A5 表述，**保持 350 行**（CONTRIBUTING 上限）。

### 修复：A6 决策项判反了，导致它拒绝删掉自己那条规则的起因句
### 修复：A6 决策项判反了，导致它拒绝删掉自己那条规则的起因句

**症状**：README 开场的「不新增事实，不改立场，不动结构。只删掉那些让中文读起来像机器
写的地方。」被 A6 判为「不改」—— 而这句正是 A6 被发明出来的原因
（`scripts/persona_experiment.py` 开头：「假说（由用户的两个实例引出）：例1『不X，不Y，只Z』」）。
规则拒绝删掉自己的起因句。

**根因**：④ 的口径写错了 —— 原文要求「在文档**另一处**（**不含起手式自身及其同段后续句**）
找到重述」，把**本句末尾的短断言排除在外**。而「不X不Y**只Z**」里的「只Z」正是这三条否定的
标准重述位。再加上要求「逐条一一对应」，就把最该删的句子判成了不改。

**修正**：④ 改为问「**删掉否定那半句后，原意是否仍有承载？**」——
把否定半句拿掉，看**剩余文字（含本句尾句）**加**文中他处**是否仍表达同一承诺；
并明确「**不要排除本句的尾句**」，以及「不必每条否定都配一句独立重述」。
两个方向的例子都写进 SKILL.md 与 `counterexamples.zh.md`，反例门里那句「❌ 不改」已改为「✅ 删」。

**同时修掉两处我自己造成的格式损伤**（本轮早前为压行数用脚本拼接所致）：
`中立立场比例66.7%` 数字粘连（已断行），以及被删掉的空行让 `>` 变成引用块起始
（该隐患在 9a85d3b 已存在，一并修掉）。SKILL.md 349 行，仍在 ≤350 预算内。

### 修复：README.en.md 未同步 + 一处算术错误

- **README.en.md 落后一整个版本**：仍写「four things」、5 条常开规则、`0.15–0.35` 下限、
  「business commentary」闸门；**完全没有自建语料章节**。它由 README.md 首行链接，是公开面。
  已与中文版逐节对齐（16 个二级标题 1:1），含语料表、撤回记录、六条规则、验收阈值、局限 5 条。
- **算术错误（两个 README 都有）**：原写「10,272 个提示词 × 6 个生成者 = 61,608 篇」，
  但 10,272 × 6 = **61,632**。原文口径是「1 位人类作者 + 5 个 LLM」，且
  **24 篇因模型拒答缺失** → 61,632 − 24 = 61,608。已改正表述，并在
  `references/evidence.md` 新增 §九把该论文语料口径钉死，防止再次复述出错。
- **补两处缺出处的数字**：「87% 文本余弦 > 0.95」（arXiv:2502.11266）与
  「中立立场 66.7% vs 人类 39.5%」（arXiv:2603.18161）此前只存在于 gitignore 的
  `.harness/`，公开仓库里无法核验。已收录进 `evidence.md` §四，并标注后者的
  **已知可靠性限制**（立场三分类由 gpt-4o 判定，无人工校验）。
- **用最终版 skill 复跑 README.md**：0 处改动。关键验证点是 A6 —— 旧版会删掉 L7
  「不新增事实，不改立场，不动结构。」，修正后的决策项 ④ 判否（「不动结构」全文指不出重述句），
  与 skill 自带反例一致。

### ⚠️ 撤回：初版两条「稳健特征」不成立

初版语料分析报了 6 条两语域稳健特征。复核时发现**两处方法缺陷**，修正后只剩 2 条。

**缺陷一：AI·口语层混用三个模型。** 原数据是 `default`×20 + `smol`×10 + `slow`×10。
模型间差异**比人机差异还大**：问句 `default` 0.452 vs `slow` 1.957（差 **105%**），
转折连词 `default` 0.000 vs `slow` 0.115（差 **300%**）。
已用单一模型全部重生成。

**缺陷二：句长统计被代码块与表格污染。** 技术博客里的标识符行（`策略文件` /
`breakthrough_platform.py` / `突破平台`）不含句末标点，被粘成一个 **411 字的假"句子"**。
修复：丢弃标签行与表格行，且只统计以 `。！？` 结尾的句子。

**受影响并已降级的结论**：

| 特征 | 初版 | 修正后 |
|---|---|---|
| 段末句长 CV | 稳健 | **仅正式语域显著**（δ=−0.44；口语 n.s.） |
| 跳步 CV | 稳健 | **仅正式语域显著**（δ=−0.52；口语 n.s.） |
| 跳步均值 | 稳健 | 同向但弱（口语 n.s.） |
| 问句 | 稳健 | 同向但弱（口语 n.s.） |
| 比喻标记 | 口语 δ=−0.35 | **无差异**（δ=−0.03/−0.15） |
| 破折号 | 正式 2.35× | 正式 **n.s.**、口语方向相反 |
| 平均句长 | 稳健 | 稳健（δ=−0.46/−0.53）✅ |
| **句长 CV** | 稳健 | 稳健（δ=−0.44/−0.38）✅ |

**存活下来的硬结论**：转折连词的语域依赖 —— 正式 **6.43×**、口语 **0.08×**、混池 **2.04×**。

### 加入：自建中文语料与两项实验

**本项目第一次拥有「原生中文人类基线」。** 此前唯一的中文证据来自英译中场景（自承源语干扰）。

语料（分层，不混池）：人类·正式 100 篇/17.4 万字（人民网）、人类·口语 82 篇/17.9 万字（博客园）、
AI·正式 40 篇、AI·口语 40 篇。脚本 `scripts/collect_corpus.py`、`analyze_corpus.py`、`stats_corpus.py`。

**关键发现**

- **混池会骗人（铁证）**：转折连词在正式语域 AI 是人类 **6.43×**，口语语域是 **0.08×（方向相反）**；
  混池算出 **2.04×** —— 两个真实语域都不成立。校准语料必须**镜像 skill 的语域闸门**。
- **根因实验推翻了「助手人格」假说**：姿态框定（助手/作者/私人通信/带受众）**不改变**起手式出现率；
  改变它的是**文体** —— 原则陈述类 **41%**、技术说明 12%、定位宣言与操作指南 0%。
- **「不X不Y」形态本身人机无频次差异** —— A6 依据的是它出现在什么文体、什么位置。

### 新增规则

- **A6 删起手式宣言** —— 开场用「不X，不Y，只Z」代替正面陈述。
  四问判定，**④「每条否定在文中别处是否被重述」是否决项**：若否定是全文唯一的承诺，删了就丢信息。
- **A3 增补「满是短句」子条** —— AI 句子更短且更均匀，改法是**合并**（禁拆分、禁改短）。
- **A4 增补「让步转折公式」** —— 「不过，X 仍面临 Y」人类 0.000 vs AI 0.186/千字。

### 修复（第二轮：A6 通过独立审查后再修三处）

- **A6 触发③ 与决策项 ④ 极性对撞（致命）**：③ 原写「每条否定在文中别处**没有**被重述」，
  ④ 写「被重述了吗？**是**→删」—— 同一属性被写成互斥前提，**交集为空，A6 永不生效**。
  已把 ③ 改为纯形态判据（文体 + 位置 + 否定平行），重述只作为 ④ 的正向条件。
- **文体清单两份文件不一致**：主文件排除「定位宣言」，反例文件把「定位说明」列为启用 →
  同一句的闸门开/闭取决于先读哪份。已统一为「原则陈述 / 取舍声明」单一白名单。
- **④ 缺匹配口径**：原写「相同措辞或等义表述」，「等义」无阈值、不可复现。
  已改为：**举证责任在改的一方** —— 必须能指出重述句的位置，指不出来就判否 → 不改。
- **竞合表缺 A6↔B5 行**：两者都吃「不是 A 而是 B」骨架，导致「一处只挂一个规则号」无法裁决。
  已补行，并给 A5/B5 行加排除。另修正一处超出证据范围的归因（把 5 种未测文体括注为 0–12%）。

### 修复（第一轮：由两个无上下文的盲测 agent 发现）

- **A6 正例与反例自相矛盾**（最严重）：A6 的「✅ 该删」示例逐字就是 README 第 7 行，
  而同一份反例门对**同一句**判「不改」—— 正例与反例在同一个文件里打架，执行者必然得出相反结论。
  已把正例换成不承载信息的样例，并把第 ④ 问提为显式否决项。
- **A6 改法违反硬边界 2**：原写「改写成正面陈述」＝添加。已改为**只删**。
- A6 启用文体含「定位说明」，但自己的数据是「定位宣言 0/5」—— 自相削弱，已移除。
- A3 未说明「连着 3 句」是否跨段（跨段合并会撞禁改清单）；未禁止跨越加粗/标题边界。已补。
- 新增**竞合规则**：A3/A6 同区域功能相反（并 vs 删）→ A6 优先；A3 不得跨越排版边界。
- 闸门 1 未给「加粗领起句」归属 → 已写明按所在段落判。
- B 组「默认不启用」此前只写在 references，SKILL.md 正文却引用 B1–B5 → 已加说明。
- `no-touch.zh.md` 修订 3 条：删除「强制句长变化」的原始表述（实测有差异，只是改法不同）、
  破折号从「中英冲突、不启用」改为「中文两源同向，可作正式语域可选规则」。

### 变更

- `README.md` 用更新后的 skill 处理并同步事实：六条规则、语域限定、验收阈值、新增自建语料章节。
- `SKILL.md` 351 行（预算 350）。

## [0.1.0] — 2026-09-17

### 修复（由本项目 README 的真实使用测试发现）

- **语域清单三处不一致**（最严重）：`SKILL.md` 闸门 1、`no-touch.zh.md` §三、
  `counterexamples.zh.md` 通用反例 1 各自列了一份停用范围，且对 README 的处理互相矛盾 ——
  同一份文件，读不同 references 会得出「改 5 处」与「改 0 处」两种相反结论。
  现闸门 1 声明为**全项目唯一定义**，references 只许引用。
- **新增「混合文档按段判」**：README、技术博客、产品文档是**容器**不是体裁。
  表格/参数/字段/代码块停用，散文段正常处理。判据：去掉格式后还剩下连贯散文吗？
- **B1/B2 的改法违反硬边界 2**：B1 原写「补一个回指词」、B2 原写「改写成有内容的判断」——
  都属**添加**。两条改法均已改为纯删除，旧写法显式标注作废。
- **A3 补「小标题序列」**：连续 3 个以上同构小标题此前**既无规则也无反例**（A3 只管相邻句，
  禁改清单管结构不可动）—— 规则树在标题层是空的。现可改**措辞**，不可改**层级顺序**；
  标证据等级 C（机制外推）。
- **A5 触发条件与自身依据矛盾**：触发器写「同一**段**里……高于全文其他段」，
  但其引用的 zake7749 第 4 条信号原文是「平均铺满**全文**」——"全文"被漏掉。
  现触发条件改为两条并列，新增「全文节奏均匀：每个章节都以短金句收尾，没有平句让读者喘气」。
- **验收阈值长文不可达**：词级编辑距离下限 0.15 在长文档上永远够不到，且与
  「改动 ≥3 处即误伤」自相矛盾。现**删掉下限只留 ≤0.35**；「≥3 处即误伤」限定为
  **输入是用户自己写的稿**，按输入来源判而非按改动数判。
- **B5 漏掉反向语序**：「……是 X，不是 Y」与「不是 A 而是 B」是同一骨架，原规则未提。已补。
- **A5 与 B5 归属不明**：一处对举可同时挂两个规则号。现明确句内对举判给 B5。
- **动作集边界补充**：用户当次明确指令可覆盖三条硬边界，但「读着像 AI」**不算**明确指令。
- 台帐增加 `说明` 列，用于标注用户越权要求与规则竞合归属理由。
- `no-touch.zh.md` 新增「关于排版层」章节：明确不处理加粗密度/标题层级/列表嵌套及理由
  （无人类侧对照证据），并要求执行者如实告知用户这部分不在射程内。
- SKILL.md 新增「A1 干净 ≠ 去味成功」，说明自省型文本观感常来自排版层。

### 变更

- `SKILL.md` 压缩至 350 行以内（预算上限）。
- `README.md` 用 renhua 自身处理：**12 处去味改动，全部为删/合并/移**（6 处小标题措辞、
  3 处节奏合并、B1/B2/B5 各 1 处），另有 1 处行数同步。信息守恒经程序化检验
  （数字、链接、关键实体计数零增减；反引号数 40→40）。
- `README.en.md` 做了**镜像同步**（标题措辞、B1/B2/B5、3 处节奏）以保持两版一致。
  ⚠️ **这不是 skill 对英文生效的证据** —— renhua 只处理中文，英文侧的 AI 特征部分方向相反。
  此处只是把中文版的结构改动同步过去，避免两版分叉。

### 实证

- 新增 `.harness/skill-verification-3.md`：两个无项目上下文的 agent 独立盲测 + 主 agent 复核。
  这是本项目第一次**真实使用**（而非自查）的记录。

## [0.1.0] — 2026-09-17

首个公开版本。

### 加入

- `skills/renhua/SKILL.md`：三条硬边界（信息守恒、动作集封闭、优先级固定）、
  四道闸门（语域 / 篇幅 / 工具残留 / 单句反例）、五条常开规则（A1–A5）、输出契约、验收方法。
- `skills/renhua/references/rules.zh.md`：五条可选规则（段首零主语评论、空转句引列表、
  伪清单结构、悬空框架定义、翻案腔），每条附人类侧地板频率与「不改的情况」。
- `skills/renhua/references/counterexamples.zh.md`：反例门中文原句表。含 18 类
  「看着命中其实不该改」的情形，以及 7 条通用反例。
- `skills/renhua/references/no-touch.zh.md`：禁改清单。18 条已证伪的流行建议、
  9 条「人类侧有地板但不能自动改」、语域豁免表、10 类绝对不可动项。
- `skills/renhua/references/evidence.md`：逐项出处、数值、语料场景、证据等级（A/B/C/P），
  含中文侧证据的真实状况说明与检测器的数学上限。
- `commands/renhua.md`：斜杠命令。
- `.claude-plugin/plugin.json`：Claude Code 插件清单。
- `CONTRIBUTING.md`：贡献指南。核心是**规则准入三条**（能指到具体位置 / 双侧有人类基线 /
  能写出 ≥3 句反例）与**证据格式**（必须标语料场景，翻译场景不算人机对照）。

### 设计要点

- **不做模型分流。** 依据：中外产模型分类准确率 66.63%≈随机；模型间可归因度 68.4%
  vs 人机 93.2%；模型指纹会被模型更新抹掉。特化改为「按文本判据命中」，判据内不含
  受模型影响的常数。
- **只做减法。** 依据：18 位职业写手 8,035 处编辑中，最常改的三类全是词句级；
  擦净七类风格毛病后 AI 检出率仅 95.5%→93.9%。
- **不用检测器验收。** 依据：`TPR ≤ FPR + TV`（TMLR Corollary 3）；
  12 个检测器中 6 个在重度改写上判定率反而下降。

### 已知问题

- 中文侧缺乏干净的原生人机对照证据。唯一具备原生中文人类基线的研究是英译中翻译场景，
  作者自承源语干扰。规则只能给方向，不能给出可信的中文实测倍率。
- 闸门的语域判定有主观性，「商评」与「技术报告」的边界不总是清楚。
- 破折号一条因中英方向冲突而默认不启用，但它在实测样本中会反复出现。

### 未采用（记录以免重复调研）

解码期分布相减（CoPA）、风格 SFT+DPO+推理期精修（MASH）、逐字复用人类原文（Frankentext）、
递归复述多轮、针对检测器的提示重优化。理由见 `references/evidence.md` 第八节。
