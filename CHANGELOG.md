# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.6.0] — 2026-09-17

> 两处门修正（A1 门收紧、**A5 补门**）+ 改动后的误伤复测。
> **复测的重要结论是否定性的**：篇级指标功效不足，测不出净效应。

### A1 门收紧：「作者隐身」≠「转述体」

复测发现 A1 仍会删掉人类博客里的**说明文**句子（如「理解这些过程对于网络协议知识非常有帮助。」），
理由是模型按段判，读到「无第一/第二人称、无呼语 → 转述体」。但**技术说明、科普、教程段落
里作者也不露面**，那不是转述体。

→ 判据改为**有没有转述框架**（"某方表示／会议指出／据…报道"），不是有没有人称。
**效果**：作者在场体上的 A1 触发 **降到 0**（24 篇博客全部不再被 A1 删）。

### A5 补门（并修掉一次自造回归）

复测中 ≥3 处误伤率一度冲到 **12.5%**，规则分布显示 **A5 在人类博客上触发 11 次**。
原因不是 A5 变了，而是**我在压缩 SKILL.md 行数时把 A5 的警告写成了
「不要单凭它触发，别处靠读感不靠统计」** —— 被合理读成「博客里可以靠读感触发 A5」，
等于**把 A5 的证据门拆掉了**。

→ 改为明确表述「**第二条只在转述体可用；随笔／技术博客停用该条**」，A5 在博客上回到 3 次。
**压缩措辞时丢掉限定语，会静默改变规则行为** —— 这是 CONTRIBUTING「保护性约束」的又一例。

### 误伤率复测：四版 CI 全部重叠

| 版本 | ≥3 处 | 95% CI | 改动均值 |
|---|---|---|---|
| §十二 原版（无门） | 4.2% | [1.2–14.0] | 0.73 |
| 加体裁门 | 2.1% | [0.4–10.9] | 0.73 |
| 仅收紧 A1 门 | 12.5% | [5.9–24.7] | 0.83 |
| **A1 收紧 + A5 加门** | **8.3%** | **[3.3–19.6]** | **0.85** |

**四版 CI 互相重叠** → n=48 无法区分这些比率（§12.6 的功效分析早就说明：需每组 ≥34 篇 × 多轮）。

→ **不要引用这张表说「改动改善了/恶化了误伤率」。** 能说的是：**没有证据表明改动把误伤
推高到不可接受的水平**（最悲观那版的 CI 上界 19.6%，仍远低于「多数文本被改坏」）。

### 已知仍存在的误伤（诚实清单）

1. **A1 在转述体上会删掉约三到四成的「其实有信息」的句子**（三版分别 29%／27%／42%，
   n=7／11／12，CI 全在 [10–68] 宽带内重叠）。**这是本项目唯一确认存在、且未消除的误伤来源** ——
   它没有被解决，只是被**限制在一个体裁内**。
2. **A3b 在分步教程体里可能产出偏长的合并句**（信息不丢，可读性下降）。现有护栏
   「一逗到底（>100 字无句末标点）就撤销」抓不住这种短得多的情况。

### 节结论

规则层改动有语料级依据（n=80/组，δ 达 +0.61~+0.74），这部分是硬的；
**篇级误伤／精度指标测不出净效应**，要判定需每组 ≥34 篇 × 多轮 —— 本轮没做，如实记录。

行数：SKILL.md 375/375。记录：`evidence.md` §二十一

## [0.5.0] — 2026-09-17

> **A3b 的触发口径重定**：旧触发几乎不生效且无选择性，换成有 2–3 倍分离度的新触发。

### 旧触发没有选择性

A3b 原触发「连着 3 句以上都在 30 字以内」，在 600 汉字窗口上实测：

| | 人类新闻 | AI新闻 | 人类博客 | AI博客 |
|---|---|---|---|---|
| 旧触发命中率 | 54.9% | 77.5% | 70.7% | **100.0%** |

**在 AI 博客上 100% 触发，人类博客也 70.7%** —— 几乎没有区分度。
与 §12.5 早已记下的分析一致：AI 的「短」来自**缺少长尾**，不是「有更多短句」，
**用「连续短句」抓它抓错了对象**。

### 真正稳健的差异在长句

| 指标 | 人类新闻 | AI新闻 | δ转述 | 人类博客 | AI博客 | δ博客 |
|---|---|---|---|---|---|---|
| 长句（>60 字）占比 | 0.147 | **0.029** | **−0.70** | 0.037 | 0.000 | **−0.41** |
| 最长句（字） | 88.3 | **60.5** | **−0.74** | 123.5 | 55.3 | **−0.65** |

最长句的 p90：人类 123 / 259，AI 77 / 70。**AI 的长句被截断在 60 字上下，人类写到 120–260。**

### 新触发：「全篇最长句 ≤70 字」

| 阈值 | 人类新闻 | AI新闻 | 差 | 人类博客 | AI博客 | 差 |
|---|---|---|---|---|---|---|
| **70** | **0.24** | **0.85** | **+0.61** | **0.35** | **0.92** | **+0.57** |

**分离度是旧触发的 2–3 倍，且两体裁方向一致。** 动作不变（合并相邻两句 —— 正好制造长句，
不增删信息），改动只在于「什么时候做」。

### 一次没生效的返工（值得记）

8 个样本端到端验证时，**第一版新触发没生效**：AI·正式 两篇 A3 触发 **0/2**，模型的理由是
「A3（无相邻句同构；**全文长短句交替正常**）」—— 它用**读感**代替了**数最长句**。
→ 改为**显式机械自检**：「数出全文最长句的字数，≤70 字 → 触发」，并写明不许用读感代替。
重测后 AI 3/4、人类 1/4，方向正确。

### 限制（必须一起说）

- 语料级判别力（n=80/组）是硬的：δ=+0.61/+0.57。
- **端到端依从性只有 8 个样本**，只能说「比旧触发更可能被执行」，**不给比率**。
- **新触发提高了 AI 文本上的改动量**（AI·正式 2.50 → 3.50 处/篇）——有意为之
  （旧触发几乎不生效、规则形同虚设），但**误伤面随之变大**：
  **§十二 的误伤测量需要在下一轮重跑**。
- 「70 字」定在 600 汉字窗口上；全篇更长时该阈值**偏保守**（对 AI 更易触发），方向安全。

行数：SKILL.md 375/375。记录：`evidence.md` §二十

## [0.4.0] — 2026-09-17

> **A3 有一处系统性误改**：SKILL.md 自己推荐的改法会导致改变原意。已修。
> 同时把 A3／A5 明确标注为**非判别项**。

### A3／A5 的定位：不是判别项

§十四 的规则体检显示 A3 与 A5 对人类/AI 几乎没有判别力（A3：人类 5% vs AI 5%；
A5：人类 2.5% vs AI 2.5%）。这两条是已发布规则，必须回答留还是去。

**先说清楚：判别力不是唯一判据。** 一条规则在两边的触发率相同，意味着它删不掉 AI 信号；
但本 skill 不是分类器，目标是让文本更好。所以真正的判据是「会伤人类文本吗」：

| | A1／A2 | A3／A5 |
|---|---|---|
| 能区分 AI 吗 | 能（但只在转述体） | **不能**（两体裁都无差异） |
| 会伤人类文本吗 | **会**（A1 删除 58% 丢信息） | 基本不会 |

→ **A1／A2 加体裁门；A3／A5 保留但标注「非判别项」** —— 立足点是机制与外部文献，
不是本项目自己的频次数据。

### A3 的「移」与 A1 的「删」风险性质不同

A1／A2 的动作是删（可能丢信息），**A3 的动作是移（换语序），理论上不丢命题内容** ——
所以 A1 的「删掉是否丢信息」判据不能直接搬给 A3。为 A3 单独设计判据：
**改动后是否改变了原意、逻辑关系或强调重点**。

### 结果：A5 安全，A3 有系统性误改

人类侧被 A3／A5 改动的**全部实例只有 7 条**（160 次运行里）—— 这两条很少碰人类文本。
7 条全部交 `slow` 盲评（不含来源与规则名）：

| 规则 | 判据 | 结果 |
|---|---|---|
| **A5**（删） | 删掉是否丢信息 | **0/2 LOSS** |
| **A3**（移） | 是否改变原意 | **2/5 CHANGED** |

**A3 那 2 例里有一例是系统性的：**

> 原文：「**同时**，保险机构在……表示了对实物资产与存量资产的偏重。」
> 改后：「保险机构**同时**在……表示了……」—— **改变了逻辑**

句首的「同时」是**句际**衔接（与上句构成并列/补充）；移到主语后变成**句内**时间副词
（"在同一时间"）。**两句话的关系被改掉了。**

**关键在于：这个误改不是随机的，而是 SKILL.md 的推荐改法直接导致的** ——
A3 原文的改法第一条就是「把其中一句的连接词挪到主语后面」，反例文件还把它当
「✅ 该打散」的示范。**按规则照做就会踩这个坑。**

### 规则修正

- **SKILL.md A3 改法**：删掉「把连接词挪到主语后」，改为「换语序／合并／拆句」，
  并加显式禁止：**句首的句际衔接词不要移入句内**。
- **`counterexamples.zh.md`**：新增反例 6（句首句际衔接词），原「✅ 该打散」示范从
  「挪连接词」改为「换语序」。
- A3／A5 各加一行**非判别项**标注。

### 一个提取失误（不影响结论，但记下来）

本轮首次提取时把改动台帐的**「位置」列当成了「说明」列**（列号差一），
打印出的「生成侧理由」其实是位置。**幸好判官用的是「句子 + 原文上下文」而不是那一列**，
判定不受影响。教训与 §16／§17／§18 同一条：**读表前列数之前先验证列映射**。

### 限制

**n=7** 是全部母体（不是抽样），但**绝对量太小**：只能定性指出系统性风险，
A3 的 2/5 与 A5 的 0/2 **都不可外推为比率**。且判官单盲（知道「有人建议这样改」），
会**低估**改变率 → 2/5 是下界。

行数：SKILL.md 375/375。
记录：`evidence.md` §十九

## [0.3.2] — 2026-09-17

> 外部校准收尾：**CCL23 的十条特征里只有两条复现**。无规则变更。

### 补齐最后四条（需词性标注的特征）

`scripts/zh_pos_features.py`：量词、文言虚字「之」、第二人称、名词重复（相邻句／全文）。
两项长度对照都做了（篇级等量窗口 + 句级按句长配对）。

| CCL23 报告 | 本项目 | 结论 |
|---|---|---|
| 量词密度 人类 1.42× | δ=+0.06，无差异 | **未复现** |
| 第二人称 AI **2.1×** | 作者在场体 **AI 更少**（δ=−0.18） | **未复现（方向相反）** |
| 相邻句名词重复 AI **2.4×** | 控句长后无差异（z=+1.1） | **未复现** |
| 相邻句实词重复 AI 1.73× | 控句长后 **AI 更少**（z=+2.8） | **未复现（方向相反）** |

⚠️ **第三次抓到检测器 bug**：**jieba 不单独标量词** —— 它把「一个」「三次」整体标成 `m`，
`q` 标签实际从不出现，初版量词统计恒为 0。改为从 `m` 词尾识别（汉语量词在末尾）。
→ 至此本项目三次「不先验证检测器就会把 bug 带进结论」（§16、§17、§18）。

### 收尾账：CCL23 十条，两条复现，八条未复现或相反

| CCL23 特征 | 本项目 | 结果 |
|---|---|---|
| 句长标准差 | 句长 CV δ=−0.44/−0.38 | ✅ **复现** |
| 连词密度 | 显式连词 δ=+0.62/+0.68 | ✅ **复现** |
| 平均句长（以字） | AI **更短** | ❌ 相反 |
| 相邻句实词重复 | 无差异 / AI 更少 | ❌ 未复现 |
| 句法树高>14 占比 | 两组均 0.0% | ⚠️ 口径不可比 |
| 并列短语数 | 0.81×（AI 更少） | ❌ 相反 |
| 选择连词 / 被字句 / 量词 | 无信号或 0 | ❌ 未复现 |
| 第二人称 / 名词短语长度 | AI **更少/更短** | ❌ 相反 |

**能复现的两条恰好是最不依赖语域的那两条**（句长标准差、连词密度），与本项目
「稳健项 = 两体裁同向」的分类吻合 → 可视为一次**外部交叉验证**。

**为什么大面积未复现**：CCL23 是**开放域问答**、GPT-3.5 时代；本项目是**新闻／技术博客**
按提示词写文章。§十四 已证**体裁决定风格**（Rallapalli 2026：体裁的塑造力强于来源）。
→ 不是该文有错，而是**它的结论不能外推到「写文章」这个任务**。

### 同时更正 §10.1 的「三条复现」

§10.1 把 CCL23 列为「句长更短」的证据之一并列了三条复现，但：
**只有句长标准差与连词密度是我们自己测出来的**；`平均句长方向`与`相邻句实词重复`
从未在自建语料上验证，复测后**都不成立**；`句法树高>14` 连度量都不同。
→ **引用外部文献当「复现」之前，必须先在自己的语料上测一遍。**

### 立场（写进 rules.zh.md）

**外部证据用来验证稳健项，不用来提供新规则。**
本轮到目前为止测试的全部外部候选（CCL23 十一条 + §十五 五条）**没有一条被采纳为新规则** ——
外部差异多数是语域／任务差异，而规则要处理的是本语料上的真实差异。

记录：`evidence.md` §十八（含 18.1 检测器验证、18.4 收尾账、18.6 限制）+ §10.1 更正②

## [0.3.1] — 2026-09-17

> **一次差点发出的假发现，和一条新的方法学规则。** 无规则变更。

### 句法复杂度类特征：几乎全是**句长伪影**

§十六装的 spaCy 顺手把 CCL23 里此前整类测不了的**依存句法特征**补上
（`scripts/syntax_features.py`）。未控制句长时，结果「像」本项目迄今最漂亮的一批：

| 指标 | δ转述 | z | δ博客 | z |
|---|---|---|---|---|
| 平均句法树高 | −0.61 | +5.4 | −0.71 | +6.1 |
| 平均依存距离 | −0.56 | +5.1 | −0.77 | +6.6 |
| 平均句最大依存距离 | −0.50 | +4.5 | −0.71 | +6.1 |
| 名词短语平均长度 | −0.40 | +3.6 | −0.51 | +4.4 |
| 动词短语平均长度 | −0.52 | +4.7 | −0.75 | +6.4 |

八个指标里五个「两体裁同向」，δ 达 −0.40~−0.77，z 高达 +6.6 —— 看去是
「**AI 中文的句法更简单**」。

**但它是假的。** AI 的句子更短（既有稳健结论），短句的树自然更浅、依存距离自然更短。
按句长分桶配对后（29 个桶，覆盖人类 1154 句 / AI 745 句）：

| 指标 | 人类 | AI | 差 | z | 判定 |
|---|---|---|---|---|---|
| 句法树高 | 4.038 | 3.901 | +0.136 | +3.2 | 残差极小 |
| 平均依存距离 | 2.619 | 2.655 | −0.037 | −1.3 | **无差异** |
| 最大依存距离 | 11.177 | 11.631 | −0.454 | −2.4 | **AI 更高（翻向）** |
| 名词短语长度 | 3.708 | 3.536 | +0.171 | +1.8 | **无差异** |

**五个「两体裁同向」的信号，控制句长后剩零个。**
→ **句法复杂度不是独立的 AI 特征，它由句长决定。**

### 新方法学规则：句级长度污染

§十五 的教训是「篇级长度污染」（用等量窗口解决）。这一节的是**句级**长度污染，
**等量窗口挡不住** —— 窗口等长不代表句长分布相同。

> **任何按句计算的量**（树高、依存距离、短语长度、每句连接词数…）在比对前，
> **必须按句长分层**或做配对。

### 顺带复核已发布规则：A4a／A4b 站得住

| 口径 | δ转述 | δ博客 |
|---|---|---|
| 每千字：并列补充 | +0.57 | +0.22 |
| **每句**：并列补充 | **+0.54** | **+0.20** |
| 每千字：转折 | +0.41 | −0.13 |
| **每句**：转折 | **+0.39** | −0.13 |

换成每句口径后 δ 几乎不变 → **A4a／A4b 不是句长伪影。**

### 与 CCL23 的第三、四处冲突（含一处口径不可对照）

- **名词短语平均长度**：CCL23 报 AI **更长**（4.816 vs 4.054）；本项目 AI **更短**（3.546）。
  ⚠️ 但**人类层几乎相同**（4.033 vs 4.054）→ AI 层相反，再次指向「问答输出 vs 写文章」。
- **句法树高 > 14 占比**：CCL23 人类 16.0%；本项目**两组都是 0.0%** —— spaCy 中文模型在
  600 字窗口的树高上限约 4–5。**两个不同的度量，不能当复现或反驳。**

**跨全部四节的规律**：CCL23 报的 AI 特征（并列短语 2.9×、选择连词 5×、被字句 2×、
第二人称 2.1×、名词短语更长）**在本项目全部未复现，且多项方向相反** ——
不是零散矛盾，而是**该文的语域/任务在本项目没有对应层**。

记录：`evidence.md` §十七（含 17.3 陷阱、17.6 限制）

## [0.3.0] — 2026-09-17

> 补上最后一个可修候选（并列短语），**未复现且方向相反** —— 并因此拿到 A4a／A4b 的机制解释。
> 同时把术语与 §十四 的体裁门统一（旧的「正式语域／口语语域」全部改名）。

### 并列短语：CCL23 的 2.9× 未复现，方向相反

§十五 留下的唯一缺口：朱君辉等 2023（CCL23）报「句均并列短语数」AI 是人类 **2.9×**
（0.251 → 0.729），且是它分类模型中贡献度最高的特征之一。这是「可修候选」里最后一条没测的
—— 并列短语在**句内**，删它属「删」，不撞禁改清单。

新增 `scripts/coord_phrases.py`，三个口径（正则代理 / jieba POS 感知 / spaCy 依存句法），
**九个变体全部给出相反方向**：

| 口径 | 指标 | 人类新闻 | AI新闻 | 比值 |
|---|---|---|---|---|
| 正则 | 并列连接合计/句 | 1.275 | 0.973 | 0.76× |
| POS strict | 并列成分/句 | 0.888 | 0.690 | 0.78× |
| **依存** | **conj 弧/句** | **2.497** | **2.021** | **0.81×** |
| 依存 | 名词并列组/句 | 0.579 | 0.474 | 0.82× |

| | 人类 | AI | 比值 |
|---|---|---|---|
| **CCL23**（该文表 2） | 0.251 | **0.729** | **2.9×（AI 更多）** |
| **本项目**（同口径 conj 弧/句，转述体） | 2.497 | 2.021 | **0.81×（AI 更少）** |

作者在场体重度显著（δ=−0.60, z=+5.1）。

**未复现的两条可查原因**：① 绝对量级差 10 倍（0.251 vs 2.497/句）→ 两家「并列短语」定义不同，
我们穷举六个变体没有一个靠近 0.251；② **语域不同** —— CCL23 是开放域问答（人类答者简短直接、
GPT 倾向分点罗列），我们的转述体是新闻、作者在场体是技术博客。§15.3 记的「选择连词未复现」
是同一根因。

> **结论：不加「删并列短语」规则。** AI 在短语层本来就**用得更少**，按 CCL23 的数字加这条，
> 会**专门去删人类写得对的地方** —— 与 §十四 A1／A2 是同一类错误。

### 顺带拿到的：A4a／A4b 的机制解释（**层级转移**）

把新数据和 §A4 既有数字并排，出现一个统一机制：

| 组 | 句级连接词/句 | 短语级并列/句 | 句级÷短语级 |
|---|---|---|---|
| 人类·正式 | 0.016 | 2.497 | 0.007 |
| **AI·正式** | **0.086** | 2.021 | **0.042** |
| 人类·口语 | 0.049 | 3.721 | 0.013 |
| AI·口语 | 0.015 | 1.655 | 0.009 |

**层级转移指数（AI ÷ 人类）：转述体 6.52×，作者在场体 0.68×。**

→ **转述体里 AI 把并列从短语层（顿号枚举）搬到了句子层（同时／此外／然而），强度 6.52×。**
人类新闻写「A、B、C」的地方，AI 写「首先 A。此外，B。同时，C。」

**这使 A4a／A4b 从经验规则变成有机制的规则**：它们处理的不是「AI 爱用连接词」这个孤立现象，
而是**一个层级转移**——删掉句首连接词后，句子会自然回到「该由短语层承担」的写法。
也解释了为什么 A4a 只在转述体成立：**层级转移只发生在转述体**（作者在场体 0.68×，无转移）。

### 方法学：检测器必须先验证再上语料

用 CCL23 自己的示意例句测检测器 ——「包括**男装、女装、童装**等」→ 1 组 3 成分（正确）；
人类例句 → 0 组（正确）。**期间修掉一个真 bug**：初版在遇到成分 token 时就 flush，
把「男装、女装、童装」错算成 **2 组 4 成分**。不先验证检测器，这个 bug 会一路带进结论。

### 术语统一

§十四 定了体裁门（转述体／作者在场体），但 SKILL.md 里还有 5 处旧命名
（「正式语域／口语语域」「两语域」）→ 全部改名，A4 的分节标题也改。
**旧命名残留 0 处。**

### 环境（已记录进脚本 docstring，下次省时间）

系统 python 是 PEP 668 externally-managed → 用 venv；`ALL_PROXY` 是 SOCKS5 但 pip 默认无
PySocks → 先装 `pysocks`；**pypi.org 与 GitHub 直连都超时** → PyPI 包走清华镜像、
GitHub 上的 spaCy 模型 wheel 经代理取；⚠️ PyPI 上同名的 `zh-core-web-sm` 是**抢注占位包**，
真模型只在 GitHub releases。

## [0.2.0] — 2026-09-17

> **一次规则集级别的修订。** 三处独立来源（自建语料语域体检、中文语体计量文献、
> 人机风格跨体裁研究）指向同一结论：**规则的语域门用错了轴**，且两条规则的误伤集中在
> 同一处。本次把所有规则**按体裁重新分档**，并更正了证据台账里两处与原文不符的表述。

### 规则集语域体检：**语域轴本身用错了**，改为按体裁分档

§十三 发现 A1 是语域依赖的。本轮把**全部六条规则**一次性过一遍（复用 160 次运行的台帐，
不新增调用），并回答了更根本的问题：我们划分语域用的那根轴对不对？

**结果（n=40/组，Fisher 精确检验 + 交互检验）**

| 规则 | 转述体差值 | 作者在场体差值 | 交互 z | 交互 p | 判定 |
|---|---|---|---|---|---|
| **A1** 零信息句 | +27.5pp ✅ | −20.0pp | **+3.59** | **0.0003** ✅ | 语域依赖 |
| **A2** 说教元评论 | **+15.0pp** ✅ | **−12.5pp** | **+3.57** | **0.0004** ✅ | **语域依赖（新发现）** |
| A3 相邻句同构 | −2.5pp | +2.5pp | −0.73 | 0.4667 | 两语域都无判别力 |
| **A4a** 转折连词 | +27.5pp ✅ | 0.0pp | **+3.59** | **0.0003** ✅ | 实为仅转述体 |
| **A4b** 并列补充 | +17.5pp ✅ | 0.0pp | **+2.91** | **0.0036** ✅ | 实为仅转述体 |
| A5 修辞密度 | −2.5pp | 0.0pp | −0.59 | 0.5553 | 无稳健信号 |
| A6 起手式宣言 | 0.0pp | 0.0pp | — | — | 从不触发（0/160） |

**A2 与 A1 完全同型，此前未被发现**：转述体 **AI 6/40 vs 人类 0/40**（p=0.026 ✅ 完美分离）；
作者在场体方向相反（人类 5/40 vs AI 0/40）。**已加同样的门。**

**A4a／A4b 名叫「两语域」，实则只在转述体够得到阈值**（A4b 在口语层触发 **0/40**）。
这不是 bug，是阈值相对密度过高（§12.5），但实际行为就是「仅转述体」。

**A3／A5／A6 在本语料上无判别力**，其中 A6 触发 0/160。**样本不足以判死**（2/40 vs 2/40 的 CI
覆盖 0 到 0.15），故保留但降级表述。A3 的 9 次命中里人类侧 4 例**全是刻意对称排比**
（「京东白条客服表示…／抖音月付客服表示…」—— 新闻刻意的对称辟谣），
与 A1/A2 同因：**形态命中 ≠ AI 特征，体裁决定「刻意」的概率**。

### 最重要的一条：语域轴用错了，改为「作者是否在场」

**三条独立证据**：

1. **定量反证**：博客的句子**比新闻更长**（人类新闻 41.7 字 vs 人类博客 74.5 字）。
   非正式口语不会比新闻书面语更长。
2. **中文语体计量对照带**（邰沁清、饶高琦 2021，CCL 2021:398-412，8 语体 × 17 特征）：
   小说 19.67、微博 22.00、散文 30.10、**新华社新闻 48.62**、公文 57.54、**学术 91.16**（平均句长/字）。
   本项目「人类·正式」41.7–51.4 与新闻吻合；「人类·口语」59.4 落在**正式带**（介于公文与学术之间），
   **远高于任何非正式语体**。
3. **机制可见**：| 模式 | 人类新闻 | 人类博客 | 倍数 |
   |---|---|---|---|
   | **第二人称（你/您）** | 0.07 | 3.12 | **45.7×** |
   | 第一人称 | 1.44 | 2.92 | 2.03× |
   → 真正的轴是**作者在不在场**：新闻是转述体（作者隐身），技术博客是对话体（作者与读者在场）。

**理论一致**：崔希亮 2020 明言「书面语不一定是写的，口语也不一定是说的」，主张用「正式／非正式」；
冯胜利的模型是「正式／非正式（共时交际距离）」×「庄典／便俗（历时距离）」，
**不是**「书面语 × 正式度」；刘艳春 2019（17 语体 × 72 特征 → 7 维度）明确写出「突破了传统
口语／书面、正式／非正式的基本对立」。

**修正**：语域门 → **体裁门**，判据是文本里可见的**人称与呼语**，不是笼统的「正式程度」：

| 体裁 | 作者位置 | A1／A2／A4a | 本项目语料 |
|---|---|---|---|
| **转述体**（新闻、报道、公报） | 隐身，转述他人 | **启用** | 人类·正式 / AI·正式 |
| **作者在场体**（随笔、技术博客、教程） | 在场，直接对读者说话 | **停用** | 人类·口语 / AI·口语 |

**机制**：转述体里作者本该隐身，AI 插入的「这告诉我们」「总之，X 意义重大」是**外来声音**，
所以显眼、且 AI 用得更多。作者在场体里，收尾金句、自我提醒、「说到底」本来就是**作者自己的
声音**，删它等于抹掉作者 —— 这也是那里 A1 的删除 **58% 丢信息**的原因。

**改名不是文字游戏**：它换了判据。原表述让模型按「正式程度」判（博客在中间地带，判不准）；
新表述按「作者在不在场、有没有在对读者说话」判。已验证能正确分流：转述体新闻得 2 处删除
（A1+A2 各一），一篇技术随笔与一篇第一人称教程均得 **0 改动**。

### 证据台账更正两处（引文与原文不符）

1. **CCL23 的句长方向被读反**。§10.1 把朱君辉等 2023 列为「句长更短」的三条证据之一，
   但该文**自己给出的方向相反**：平均句长（以字为单位）**人类 40.893 vs GPT 42.396**，
   正文亦明言「ChatGPT 生成的句子字数更多，词数却更少」。已从核对仓库内 PDF
   `papers/ling/zhu-2023-...pdf` 表 2 确认。**这与其 `CONTRIBUTING.md` 记的
   「引用数字前必须读表头」是同一类错误** —— 当时只取了该表一列数字，未核对该文自述方向。
   该文支持的是**句长更均匀**与**连词更多**，不是平均句长更短。
2. **「体裁平化 (genre flattening)」术语与证据等级双错**。正确术语是
   **register leveling**；且 Muñoz-Ortiz 2024 全文**只有一句话**提及它，**无数据、无引用、无检验**，
   该文**只测英文新闻、从未比较任何两个语域** → 降级为**机制外推**，不可作「已实测证据」。
   同时补入**正面反证**：**Rallapalli et al. 2026**（arXiv:2604.14111，11 个 LLM × **8 个体裁**
   × 4 种解码，Biber 特征）结论「**genre exerts a stronger influence on stylistic features than
   the source itself**」—— 体裁的塑造力**强于**人机来源，即 LLM **会**随体裁移动风格。
   这**独立支持**本项目的「先分体裁再谈规则」，但**削弱**「AI 有跨体裁统一指纹」的强表述。

> **孤证声明**：「AI 在作者在场体反而更像人」这一点，外部文献**没有同型报告**；
> 主流方向相反（4 篇独立反证），唯一明确的跨语料方向翻转（Nature Comm 17:456）作者归因于
> **语料构造**而非语域。→ A1／A2 的体裁反向目前**是本项目的孤立发现**，无外部效度支撑，
> 需后续独立复现。

### 外部候选特征复测：五个里三个未复现、一个不可修

一轮外部校准（中文学界期刊 + 中文 NLP 会议 + **仓库内已存 PDF 的未读表格**）产出一批
「文献有证据、本项目未测」的候选。全部在自建语料上复测（**每篇取 600 汉字等量窗口**，
因为 AI·正式 篇均 837 汉字 vs 人类 2230，TTR／段数这类量对长度极敏感）。

| 特征 | 文献方向 | 本项目（控长度后） | 结论 |
|---|---|---|---|
| **TTR / 仅一次字占比** | 方向分裂（CCL23 报人类高；英文侧报 AI 高） | **AI 更高**（δ=+0.57） | 与英文侧同向、与 CCL23 反向 → **冲突**，不足以立规则 |
| **段落数 / 段长** | CCL23：AI 段落更多更短 | 一致，但**只在作者在场体显著**（+0.45/−0.57） | **复现但不可修** —— 撞禁改清单 |
| **选择连词** | CCL23：AI **5×** | 转述体 AI **0.000**（人类 0.159） | **未复现**（该文是问答语域） |
| **被字句** | CCL23：AI **2×** | 无信号 | **未复现** |
| 被字句／把字句／段长 CV | — | 均无信号 | — |

**结论**：五个候选里三个未复现、一个不可修（结构）、一个冲突。未复现的**原因可查**
（语域不同、长度未控）。→ 已把方法学要点写进 `rules.zh.md` 的「不在这里的规则」表：
**外部特征必须先对齐同体裁，且对 TTR／段数这类长度敏感量做等量窗口**。

### A1 是**语域依赖**的（重大规则修正）：口语/随笔语域整体停用

§十二 的误伤统计显示 A1 占人类侧改动的 ~75%，本轮专门测它，样本扩到 **每组 40 篇**
（按 §12.6 功效分析：检出 0.5 处差异需 ≈34 篇/组）。

**结论：A1 一直是「语域中立」的，这是错的 —— 它与 A4a 同型，两语域方向相反。**

**篇级**（160 次运行，600 汉字清洗后窗口，`seed=7`）

| 组 | A1 触发率 | 95% CI |
|---|---|---|
| 人类·正式 | 15.0% (6/40) | [7.1–29.1] |
| **AI·正式** | **42.5%** (17/40) | [28.5–57.8] |
| **人类·口语** | **32.5%** (13/40) | [20.1–48.0] |
| AI·口语 | 12.5% (5/40) | [5.5–26.1] |

| 语域 | AI − 人类 | Fisher |
|---|---|---|
| **正式** | **+27.5pp** | **p = 0.0126** ✅ |
| **口语** | **−20.0pp** | p = 0.0591（方向相反） |
| **交互** | **+47.5pp** | **z = 3.59, p = 0.0003** ✅ |

合并两语域后差距只有 **+3.8pp** —— 会掩盖两个相反的真相（与 A4a「混池 2.04×」同一失效模式）。
**窗口位置混淆已排除**（分层后模式不变；含文末的篇数仅 1–3/40）。

**删除句级**（盲评，48 条，判官用独立模型且不含来源）

| 来源 | 删除数 | 误删率 |
|---|---|---|
| **人类合计** | 19 | **47%** [27–68] |
| — 其中 人类·口语 | 12 | **58%** [32–81] |
| **AI 合计** | 29 | **28%** [15–46] |

→ A1 在人类文本上的删除**近一半丢信息**；口语语域 **58%**，与篇级「口语反向」互证。
→ **判官准确率人工抽查约 90%**（逐条核对人类·口语 12 例：LOSS 组 6/7 认同，SAFE 组 5/5 认同），
量级站得住。

**规则修正**：A1 加语域门 —— **只在正式语域启用**；随笔、技术博客、口语**停用**。
正文规则标题由「六条常开」改为「六条」（现 A1 与 A4a 两条带门）。

**为什么停用而不是收紧**：口语语域 A1 不是「不够准」而是**方向相反** ——
它在人类文本上触发更多（32.5% vs 12.5%）且删得更差。继续启用等于专门去改人类写得对的地方。

**已验证生效**：同一句零信息收束放在两种语域 —— 随笔输入得 **0 改动**（模型明确写「A1 按语域门
停用」），正式输入正确命中并**区分**了公文的「执行口径结尾」反例。

**两个设计决定**：600 汉字窗口（受 AI·正式 最长 1076 汉字所限，是唯一能让四组都取到 40 篇的设置）；
窗口必须**先过 `clean()`** —— 不清洗时 AI·口语 窗口汉字占比仅 18%（大量代码块），
等于在测代码处理而不是 A1。

**限制**：单盲（判官知道该句被建议删掉）；600 汉字是折中；单次运行，篇级比率应视为量级
而非精确值（交互方向的显著性 z=3.59 远超噪声）。

行数：SKILL.md 375/375（A1 加门的同时压缩了它自身与三处冗余）。
记录：`evidence.md` §十三 + `counterexamples.zh.md` A1 区（含口语语域停用说明）。

### 误伤率首次实测：≈4%；A1 是主要来源；A3b 的代码级触发被反例门全部吸收

此前所有测量都是**聚合**的（AI 组 vs 人类组的均值差），从未测过**单篇**——
而聚合显著不等于单篇安全。本轮补上这个唯一未量化的指标。

新增 `scripts/false_positive.py`（代码级触发率）+ LLM 端到端协议（72 次运行）。

**结果**（人类原创 24 篇：12 人民网 + 12 博客园，600–1500 汉字，seed=42）

| 指标 | 值 |
|---|---|
| 单次运行 ≥3 处（CONTRIBUTING 定义的误伤），池化 48 次 | **4.2%** [1.2–14.0] |
| 每篇改动数 | 均值 **0.73**，中位 **0**，范围 0–3 |
| 零改动的篇数 | 24 篇里 **10 篇** |

**归因**：A1 零信息句占人类侧改动的 **~75%**。逐例核对分两类 —— 合理删除（与导语逐字重复的
引语、同义复述）与真误伤（技术博客的**收尾金句**，如「一个字段，保不住一整张表」，那是语体
签名不是复述）。A4b 造成 1 例真误伤：原文是「首先…其次…**此外**…」的**列举序列**。

**两处修订**（依据是逐例核对，**不是**那个未达功效的 A/B）：

- **A1 新增反例门**：个人随笔/技术博客里**带作者口吻或具体意象**的收束句不删
- **A4b 新增反例门**：与「首先／其次／最后／第一／第二」构成**列举序列**时不删

**两条重要否定结果**

1. **A3b 的代码级触发率全部被反例门吸收**：代码代理测出 A3b 在 **23.8%** 的人类正式文本上
   触发（且方向是反的：AI 正式只有 12.5%），看起来最危险 —— 但端到端 72 次运行里
   **在人类文本上一次都没触发**。这是对闸门 4 的一次实测验证：
   代码级触发率是误伤率的**很松的上界**，落地靠的是判断层。

2. **A4a／A4b 的触发条件按字面几乎够不到**。频次差异是真的，但阈值相对密度过高：
   AI 正式每段转折词均值只有 **0.09** 个、每篇 **0.55** 个，「段内 ≥2 次」**永不发生**。
   A3b 的根因不同：AI 句长确实更短（40.1 vs 48.4 字），但**「≤30 字占比」两边几乎相同**
   （35.8% vs 36.4%）—— AI 的「短」来自**缺少长尾**，不是「有更多短句」，
   用「连续 3 句 ≤30 字」抓它抓错了对象。
   ⚠️ 触发口径（文内密度 vs 段内聚集）确有歧义，**已记录但故意未改** ——
   改成密度型会让规则真正开始工作，也就**可能抬高误伤**，而当前样本量不足以验证。

**方法学限制（必须随结论一起说）**：**运行方差大于版本差异**。同一批 24 篇、同一提示词，
v2 自身两次重复的平均绝对差 **0.62**，而 v1 vs v2 是 **0.75–0.88** —— 同一量级。
24 篇里两次重复只有 14 篇完全一致。SD=0.74 → 检出 0.5 处差异需每组 **≈34 篇**。
**所以不要引用 A/B 说「修订改善了误伤率」，那个结论证据不足。**

**一处不算误伤的误伤**：人民网正文后带页面构件（「分享让更多人看到」、客户端下载列表、
`-->` 残留），T0 删掉它们是**正确行为**。做误伤统计时必须排除，否则会高估。

完整记录：`evidence.md` §十二（含 12.3 逐例归因表、12.5 触发口径分析、12.6 功效分析）。

### 调研结论：韵律线立项试做后**关闭**（不产出可用机制）

用户授权试做「韵律/庄雅度」这条线 —— 文献调研把它列为理论上唯一**非词表机制**
（汉语有音节组配约束，某些组配天生「不上口」，与用词无关）。**结论是不能。**
完整记录见 `evidence.md` §十一，复现脚本 `scripts/prosody_features.py`。

| 测量 | 结果 | 有效性 |
|---|---|---|
| **音节组配四格分布**（不需词表） | 两语域**全部无差异**（0.93–1.13×，无稳健项） | ✅ 有效 → **无信号** |
| 述宾/定中结构内组配（需 POS） | 唯一信号：述宾[2+1] 口语 1.74×，**正式 1.03× n.s.** | ❌ **测量无效** |
| 合偶词配单音节（封闭小类） | AI 正式仅 3 例，样本不足 | ⚠️ 无法判定 |

**核心结论**：**AI 中文完整复现了人类中文的音节组配分布** —— 四格组配的两语域倍率
全在 0.93–1.13× 之间。

**为什么结构内组配的测量无效**（已写进脚本顶部，防止重做）：`jieba.posseg` 的
「动词+名词」邻接会把 **VN+N 定中复合词**误判为述宾。实测高频误判项
「提示词」「优化器」「工作流」「传承人」「测报灯」**全是名词**。它们按话题词表分布，
所以初次测出的「述宾[2+1] 口语 1.77×」实际是**话题差异**，不是韵律违规。
可靠的述宾识别需要依存句法分析器，本项目没有。

**另有两处拿不到**：冯胜利 2005「文白比例」需**人工判定**哪句「较文」
（王力《小气》12 逗中 5 处 → 40%）；2008《庄雅度自动测量》的算式需 350–500 条
嵌偶词/合偶词**词表**（论文付费，OA 版附录只摘 4 对【书】/【白】对照）。

**即使有信号也不适用**：韵律修正的动作是**替换**（「*种植树」→「种树」/「种植树木」），
需要知道替代说法，超出本 skill 的「删/合并/移/还原」动作集。

**行数预算同步放宽**：350 → **360**（`CONTRIBUTING.md`）。A4b 拆条后 A4 需容纳两条
子规则，350 行会把规则压到读不懂。放宽的是总行数，不是信息密度。

### 新增：A4b 并列补充连词（两语域通用），A4 拆为 a/b；A5 补中文修辞学依据

**背景**：回归中文学界本身做了一轮文献调研（六路 + 第二轮六路补漏），**推翻了 `evidence.md`
第五节「中文句法现象级证据 = 0 篇」的旧断言** —— 当时只搜英文文献与 NLP 会议，漏掉了中文学界
自己的期刊。实际存在多篇直接的中文机对比研究。详见 `evidence.md` §十。

**改动一：A4 拆成 A4a / A4b**

原 A4 只有一条规则（转折连词），且因语域依赖被限制在正式语域。拆开连词子类后发现
**转折类与并列补充类的语域行为不同**：

| 子类 | 正式 | 口语 | 结论 |
|---|---|---|---|
| 转折 | 6.43×（p<0.001） | 0.08×（p<0.01） | 方向相反 → 仍仅正式语域 |
| **并列补充** | **5.72×（p<0.001）** | **3.33×（p<0.01）** | **两语域同向 → 两类语域都启用** |

→ A4b 是 A4 里第一个**两语域通用**的子条，把一条被语域困住的规则救回了一半。

**关键定义决定**：`以及` **不计入**。它是短语内并列词（"A、B 以及 C"），不是句际衔接标记；
计入会抬高人类侧、把倍率从 5.72× 稀释到 2.63×。A4b 只取**句首**形态。

**独立印证**：Kong & Macken 2025（MT Summit, arXiv:2506.22050）用**人民日报+新华网**
原生中文对比 11 个翻译引擎，其 top-10 判别特征含 `ratio_paraConj`（并列连词）与
`ratio_advrstvConj`（转折连词）—— 与本项目 A4a/A4b 两条**独立地**吻合。该文语料与本项目
人类正式语料**同源**。

**改动二：A5 补中文修辞学依据**

`证据.md` A5 原只有 P 级（从业者博客）。中文修辞学有**同构的经典表述**（王希杰《汉语修辞学》
风格原则：「只有适度才能不让……繁丰变成冗杂，简洁变成干枯」），与「删到留下文眼」是同一判断。
同时新增一条**必须避开的误配**：陈望道的「积极修辞／消极修辞」**不等于**「繁复／简洁」或
「好／坏」；消极修辞是正面标准，不是本动作集。

**同时修正**：A3 里「段末句长 CV 只在正式语域显著」是 A5 的证据，误挂在 A3，已移除（去重）。

**验证**：CONTRIBUTING 要求的三项负向测试全部通过 ——
① 人类自写随笔 → **0 处改动**（A3 命中但被反例门挡下）
② 公文通知 → **0 处改动**（闸门 1 生效）
③ AI 稿（含 3 处段首「同时/此外」）→ A4b 命中 3 处、A1 命中 1 处，改动全部可指到规则号
另加 A4b 检测契约 9 例（含「以及」反例）全部通过。

**行数**：SKILL.md 新增规则的同时压缩了 A4/A5 表述，**保持 350 行**（CONTRIBUTING 上限）。

### 修复：A6 决策项判反了，导致它拒绝删掉自己那条规则的起因句
### 修复：A6 决策项判反了，导致它拒绝删掉自己那条规则的起因句

**症状**：README 开场的「不新增事实，不改立场，不动结构。只删掉那些让中文读起来像机器
写的地方。」被 A6 判为「不改」—— 而这句正是 A6 被发明出来的原因
（`scripts/persona_experiment.py` 开头：「假说（由用户的两个实例引出）：例1『不X，不Y，只Z』」）。
规则拒绝删掉自己的起因句。

**根因**：④ 的口径写错了 —— 原文要求「在文档**另一处**（**不含起手式自身及其同段后续句**）
找到重述」，把**本句末尾的短断言排除在外**。而「不X不Y**只Z**」里的「只Z」正是这三条否定的
标准重述位。再加上要求「逐条一一对应」，就把最该删的句子判成了不改。

**修正**：④ 改为问「**删掉否定那半句后，原意是否仍有承载？**」——
把否定半句拿掉，看**剩余文字（含本句尾句）**加**文中他处**是否仍表达同一承诺；
并明确「**不要排除本句的尾句**」，以及「不必每条否定都配一句独立重述」。
两个方向的例子都写进 SKILL.md 与 `counterexamples.zh.md`，反例门里那句「❌ 不改」已改为「✅ 删」。

**同时修掉两处我自己造成的格式损伤**（本轮早前为压行数用脚本拼接所致）：
`中立立场比例66.7%` 数字粘连（已断行），以及被删掉的空行让 `>` 变成引用块起始
（该隐患在 9a85d3b 已存在，一并修掉）。SKILL.md 349 行，仍在 ≤350 预算内。

### 修复：README.en.md 未同步 + 一处算术错误

- **README.en.md 落后一整个版本**：仍写「four things」、5 条常开规则、`0.15–0.35` 下限、
  「business commentary」闸门；**完全没有自建语料章节**。它由 README.md 首行链接，是公开面。
  已与中文版逐节对齐（16 个二级标题 1:1），含语料表、撤回记录、六条规则、验收阈值、局限 5 条。
- **算术错误（两个 README 都有）**：原写「10,272 个提示词 × 6 个生成者 = 61,608 篇」，
  但 10,272 × 6 = **61,632**。原文口径是「1 位人类作者 + 5 个 LLM」，且
  **24 篇因模型拒答缺失** → 61,632 − 24 = 61,608。已改正表述，并在
  `references/evidence.md` 新增 §九把该论文语料口径钉死，防止再次复述出错。
- **补两处缺出处的数字**：「87% 文本余弦 > 0.95」（arXiv:2502.11266）与
  「中立立场 66.7% vs 人类 39.5%」（arXiv:2603.18161）此前只存在于 gitignore 的
  `.harness/`，公开仓库里无法核验。已收录进 `evidence.md` §四，并标注后者的
  **已知可靠性限制**（立场三分类由 gpt-4o 判定，无人工校验）。
- **用最终版 skill 复跑 README.md**：0 处改动。关键验证点是 A6 —— 旧版会删掉 L7
  「不新增事实，不改立场，不动结构。」，修正后的决策项 ④ 判否（「不动结构」全文指不出重述句），
  与 skill 自带反例一致。

### ⚠️ 撤回：初版两条「稳健特征」不成立

初版语料分析报了 6 条两语域稳健特征。复核时发现**两处方法缺陷**，修正后只剩 2 条。

**缺陷一：AI·口语层混用三个模型。** 原数据是 `default`×20 + `smol`×10 + `slow`×10。
模型间差异**比人机差异还大**：问句 `default` 0.452 vs `slow` 1.957（差 **105%**），
转折连词 `default` 0.000 vs `slow` 0.115（差 **300%**）。
已用单一模型全部重生成。

**缺陷二：句长统计被代码块与表格污染。** 技术博客里的标识符行（`策略文件` /
`breakthrough_platform.py` / `突破平台`）不含句末标点，被粘成一个 **411 字的假"句子"**。
修复：丢弃标签行与表格行，且只统计以 `。！？` 结尾的句子。

**受影响并已降级的结论**：

| 特征 | 初版 | 修正后 |
|---|---|---|
| 段末句长 CV | 稳健 | **仅正式语域显著**（δ=−0.44；口语 n.s.） |
| 跳步 CV | 稳健 | **仅正式语域显著**（δ=−0.52；口语 n.s.） |
| 跳步均值 | 稳健 | 同向但弱（口语 n.s.） |
| 问句 | 稳健 | 同向但弱（口语 n.s.） |
| 比喻标记 | 口语 δ=−0.35 | **无差异**（δ=−0.03/−0.15） |
| 破折号 | 正式 2.35× | 正式 **n.s.**、口语方向相反 |
| 平均句长 | 稳健 | 稳健（δ=−0.46/−0.53）✅ |
| **句长 CV** | 稳健 | 稳健（δ=−0.44/−0.38）✅ |

**存活下来的硬结论**：转折连词的语域依赖 —— 正式 **6.43×**、口语 **0.08×**、混池 **2.04×**。

### 加入：自建中文语料与两项实验

**本项目第一次拥有「原生中文人类基线」。** 此前唯一的中文证据来自英译中场景（自承源语干扰）。

语料（分层，不混池）：人类·正式 100 篇/17.4 万字（人民网）、人类·口语 82 篇/17.9 万字（博客园）、
AI·正式 40 篇、AI·口语 40 篇。脚本 `scripts/collect_corpus.py`、`analyze_corpus.py`、`stats_corpus.py`。

**关键发现**

- **混池会骗人（铁证）**：转折连词在正式语域 AI 是人类 **6.43×**，口语语域是 **0.08×（方向相反）**；
  混池算出 **2.04×** —— 两个真实语域都不成立。校准语料必须**镜像 skill 的语域闸门**。
- **根因实验推翻了「助手人格」假说**：姿态框定（助手/作者/私人通信/带受众）**不改变**起手式出现率；
  改变它的是**文体** —— 原则陈述类 **41%**、技术说明 12%、定位宣言与操作指南 0%。
- **「不X不Y」形态本身人机无频次差异** —— A6 依据的是它出现在什么文体、什么位置。

### 新增规则

- **A6 删起手式宣言** —— 开场用「不X，不Y，只Z」代替正面陈述。
  四问判定，**④「每条否定在文中别处是否被重述」是否决项**：若否定是全文唯一的承诺，删了就丢信息。
- **A3 增补「满是短句」子条** —— AI 句子更短且更均匀，改法是**合并**（禁拆分、禁改短）。
- **A4 增补「让步转折公式」** —— 「不过，X 仍面临 Y」人类 0.000 vs AI 0.186/千字。

### 修复（第二轮：A6 通过独立审查后再修三处）

- **A6 触发③ 与决策项 ④ 极性对撞（致命）**：③ 原写「每条否定在文中别处**没有**被重述」，
  ④ 写「被重述了吗？**是**→删」—— 同一属性被写成互斥前提，**交集为空，A6 永不生效**。
  已把 ③ 改为纯形态判据（文体 + 位置 + 否定平行），重述只作为 ④ 的正向条件。
- **文体清单两份文件不一致**：主文件排除「定位宣言」，反例文件把「定位说明」列为启用 →
  同一句的闸门开/闭取决于先读哪份。已统一为「原则陈述 / 取舍声明」单一白名单。
- **④ 缺匹配口径**：原写「相同措辞或等义表述」，「等义」无阈值、不可复现。
  已改为：**举证责任在改的一方** —— 必须能指出重述句的位置，指不出来就判否 → 不改。
- **竞合表缺 A6↔B5 行**：两者都吃「不是 A 而是 B」骨架，导致「一处只挂一个规则号」无法裁决。
  已补行，并给 A5/B5 行加排除。另修正一处超出证据范围的归因（把 5 种未测文体括注为 0–12%）。

### 修复（第一轮：由两个无上下文的盲测 agent 发现）

- **A6 正例与反例自相矛盾**（最严重）：A6 的「✅ 该删」示例逐字就是 README 第 7 行，
  而同一份反例门对**同一句**判「不改」—— 正例与反例在同一个文件里打架，执行者必然得出相反结论。
  已把正例换成不承载信息的样例，并把第 ④ 问提为显式否决项。
- **A6 改法违反硬边界 2**：原写「改写成正面陈述」＝添加。已改为**只删**。
- A6 启用文体含「定位说明」，但自己的数据是「定位宣言 0/5」—— 自相削弱，已移除。
- A3 未说明「连着 3 句」是否跨段（跨段合并会撞禁改清单）；未禁止跨越加粗/标题边界。已补。
- 新增**竞合规则**：A3/A6 同区域功能相反（并 vs 删）→ A6 优先；A3 不得跨越排版边界。
- 闸门 1 未给「加粗领起句」归属 → 已写明按所在段落判。
- B 组「默认不启用」此前只写在 references，SKILL.md 正文却引用 B1–B5 → 已加说明。
- `no-touch.zh.md` 修订 3 条：删除「强制句长变化」的原始表述（实测有差异，只是改法不同）、
  破折号从「中英冲突、不启用」改为「中文两源同向，可作正式语域可选规则」。

### 变更

- `README.md` 用更新后的 skill 处理并同步事实：六条规则、语域限定、验收阈值、新增自建语料章节。
- `SKILL.md` 351 行（预算 350）。

## [0.1.1] — 2026-09-17

### 修复（由本项目 README 的真实使用测试发现）

- **语域清单三处不一致**（最严重）：`SKILL.md` 闸门 1、`no-touch.zh.md` §三、
  `counterexamples.zh.md` 通用反例 1 各自列了一份停用范围，且对 README 的处理互相矛盾 ——
  同一份文件，读不同 references 会得出「改 5 处」与「改 0 处」两种相反结论。
  现闸门 1 声明为**全项目唯一定义**，references 只许引用。
- **新增「混合文档按段判」**：README、技术博客、产品文档是**容器**不是体裁。
  表格/参数/字段/代码块停用，散文段正常处理。判据：去掉格式后还剩下连贯散文吗？
- **B1/B2 的改法违反硬边界 2**：B1 原写「补一个回指词」、B2 原写「改写成有内容的判断」——
  都属**添加**。两条改法均已改为纯删除，旧写法显式标注作废。
- **A3 补「小标题序列」**：连续 3 个以上同构小标题此前**既无规则也无反例**（A3 只管相邻句，
  禁改清单管结构不可动）—— 规则树在标题层是空的。现可改**措辞**，不可改**层级顺序**；
  标证据等级 C（机制外推）。
- **A5 触发条件与自身依据矛盾**：触发器写「同一**段**里……高于全文其他段」，
  但其引用的 zake7749 第 4 条信号原文是「平均铺满**全文**」——"全文"被漏掉。
  现触发条件改为两条并列，新增「全文节奏均匀：每个章节都以短金句收尾，没有平句让读者喘气」。
- **验收阈值长文不可达**：词级编辑距离下限 0.15 在长文档上永远够不到，且与
  「改动 ≥3 处即误伤」自相矛盾。现**删掉下限只留 ≤0.35**；「≥3 处即误伤」限定为
  **输入是用户自己写的稿**，按输入来源判而非按改动数判。
- **B5 漏掉反向语序**：「……是 X，不是 Y」与「不是 A 而是 B」是同一骨架，原规则未提。已补。
- **A5 与 B5 归属不明**：一处对举可同时挂两个规则号。现明确句内对举判给 B5。
- **动作集边界补充**：用户当次明确指令可覆盖三条硬边界，但「读着像 AI」**不算**明确指令。
- 台帐增加 `说明` 列，用于标注用户越权要求与规则竞合归属理由。
- `no-touch.zh.md` 新增「关于排版层」章节：明确不处理加粗密度/标题层级/列表嵌套及理由
  （无人类侧对照证据），并要求执行者如实告知用户这部分不在射程内。
- SKILL.md 新增「A1 干净 ≠ 去味成功」，说明自省型文本观感常来自排版层。

### 变更

- `SKILL.md` 压缩至 350 行以内（预算上限）。
- `README.md` 用 renhua 自身处理：**12 处去味改动，全部为删/合并/移**（6 处小标题措辞、
  3 处节奏合并、B1/B2/B5 各 1 处），另有 1 处行数同步。信息守恒经程序化检验
  （数字、链接、关键实体计数零增减；反引号数 40→40）。
- `README.en.md` 做了**镜像同步**（标题措辞、B1/B2/B5、3 处节奏）以保持两版一致。
  ⚠️ **这不是 skill 对英文生效的证据** —— renhua 只处理中文，英文侧的 AI 特征部分方向相反。
  此处只是把中文版的结构改动同步过去，避免两版分叉。

### 实证

- 新增 `.harness/skill-verification-3.md`：两个无项目上下文的 agent 独立盲测 + 主 agent 复核。
  这是本项目第一次**真实使用**（而非自查）的记录。

## [0.1.0] — 2026-09-17

首个公开版本。

### 加入

- `skills/renhua/SKILL.md`：三条硬边界（信息守恒、动作集封闭、优先级固定）、
  四道闸门（语域 / 篇幅 / 工具残留 / 单句反例）、五条常开规则（A1–A5）、输出契约、验收方法。
- `skills/renhua/references/rules.zh.md`：五条可选规则（段首零主语评论、空转句引列表、
  伪清单结构、悬空框架定义、翻案腔），每条附人类侧地板频率与「不改的情况」。
- `skills/renhua/references/counterexamples.zh.md`：反例门中文原句表。含 18 类
  「看着命中其实不该改」的情形，以及 7 条通用反例。
- `skills/renhua/references/no-touch.zh.md`：禁改清单。18 条已证伪的流行建议、
  9 条「人类侧有地板但不能自动改」、语域豁免表、10 类绝对不可动项。
- `skills/renhua/references/evidence.md`：逐项出处、数值、语料场景、证据等级（A/B/C/P），
  含中文侧证据的真实状况说明与检测器的数学上限。
- `commands/renhua.md`：斜杠命令。
- `.claude-plugin/plugin.json`：Claude Code 插件清单。
- `CONTRIBUTING.md`：贡献指南。核心是**规则准入三条**（能指到具体位置 / 双侧有人类基线 /
  能写出 ≥3 句反例）与**证据格式**（必须标语料场景，翻译场景不算人机对照）。

### 设计要点

- **不做模型分流。** 依据：中外产模型分类准确率 66.63%≈随机；模型间可归因度 68.4%
  vs 人机 93.2%；模型指纹会被模型更新抹掉。特化改为「按文本判据命中」，判据内不含
  受模型影响的常数。
- **只做减法。** 依据：18 位职业写手 8,035 处编辑中，最常改的三类全是词句级；
  擦净七类风格毛病后 AI 检出率仅 95.5%→93.9%。
- **不用检测器验收。** 依据：`TPR ≤ FPR + TV`（TMLR Corollary 3）；
  12 个检测器中 6 个在重度改写上判定率反而下降。

### 已知问题

- 中文侧缺乏干净的原生人机对照证据。唯一具备原生中文人类基线的研究是英译中翻译场景，
  作者自承源语干扰。规则只能给方向，不能给出可信的中文实测倍率。
- 闸门的语域判定有主观性，「商评」与「技术报告」的边界不总是清楚。
- 破折号一条因中英方向冲突而默认不启用，但它在实测样本中会反复出现。

### 未采用（记录以免重复调研）

解码期分布相减（CoPA）、风格 SFT+DPO+推理期精修（MASH）、逐字复用人类原文（Frankentext）、
递归复述多轮、针对检测器的提示重优化。理由见 `references/evidence.md` 第八节。
