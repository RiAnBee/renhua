# 参与贡献

这个项目最核心的资产不是规则本身，而是**规则的准入标准**。所以贡献方式也围绕这个来。

## 欢迎的贡献

| 类型 | 说明 |
|---|---|
| **新证据** | 指出某条规则的依据更强 / 更弱 / 方向错了 |
| **反例** | 给出「看着命中规则、其实不该改」的真实中文句子 |
| **实测数据** | 在真实中文语料上复算某条规则的人机频率比 |
| **失效报告** | skill 把一段**人写的中文**改坏了 |
| **错误更正** | 引用错了、数字抄错了、把 A 的结论算到 B 头上 |

## 特别欢迎：反例

理由：本项目的圆桌讨论里，红队指出**头号失效模式不是漏改，而是把人类正常写法改坏**。

人类在这些方面用得**比 AI 更多**（实测）：

- 比喻 **2.4×**（独立成段 8×）
- 正文问句 **17×**
- 句内排比 **0.61×**（人类更多）

所以一条没有反例的规则是**危险的规则**。如果你发现某条规则会误伤正常中文，
请直接给原句 —— 那比任何理论讨论都有用。

---

## 加一条规则的门槛

**三条必须同时满足**（缺任何一条，规则会被拒绝）：

### 1. 能指到具体位置

规则必须能回答「**改哪一句 / 哪个词**」。

检查：拿 10 段真实中文，规则能否指出具体位置？只能给出「整体感觉」的规则不算数。

### 2. 双侧有人类基线

必须给出**人类侧的对照数字**，不能只有「AI 侧很多」。

⚠️ **特别注意语料场景**。以下情况**不算**有效证据：

- 只有 AI 侧频率，没有人类侧 → 不算
- 人类侧和 AI 侧不是同一生成任务（例：人类写原生中文 vs 机器翻译中文）→ **不算**
- 两侧都是机器译文（测的是翻译腔强度，不是人机差异）→ **不算**

**这是一个真实踩过的坑**：本项目曾把一篇论文表格里的「低翻译腔 vs 高翻译腔」两列
误读为「人类 vs AI」，据此得出的「中英代词方向相反」结论是错的。
提交证据时请**贴出表格的原始表头与 caption**。

### 3. 能写出 ≥3 句中文原句反例

写不出反例的规则，说明触发条件太宽。**写不出就删这条规则。**

---

## 证据格式

提交 issue 或 PR 时带上：

```
规则：A4 收敛显性衔接
主张：转折连词 AI >2× 人类
出处：arXiv:2506.22050
语料场景：英译中新闻（机器侧是翻译，作者自承源语干扰）
人类侧：OCN（人民日报 / 新华网），原生中文
AI 侧：同一批新闻的机器译文
数值：>2×
证据等级：A（带源语干扰）
已知反例：
  1. 条件句里的「但是」删了会改变逻辑关系
  2. 法律条款里的让步与例外标记
  3. 学术论证里区分强弱转折的地方
```

**证据等级**（写进 `references/evidence.md`）：

| 等级 | 含义 |
|---|---|
| A | 中文语料实测，有原生中文人类基线 |
| B | 跨语言机制，多篇独立互证，语料非中文 |
| C | 单一来源 / 语料窄 / 与其它证据冲突 |
| P | 实践观察（从业者方法论），无实验 |

**C 级证据不会被拒，但会被显式标注。** 本项目大量使用 C 级与 P 级证据 ——
关键是**如实标注**，而不是假装它们和 A 级一样硬。

---

## 不会接受的改动

### 1. 加动作

动作集是 **{删, 合并, 移, 还原}**。

**「添加」被禁止** —— 补例子、补细节、补数据、补过渡、把抽象写具体、补情绪描写，都不行。
理由是信息守恒：补的内容来自模型而不是原文，那是编造。

### 2. 引入模型分流

按厂商 / 模型版本分规则表，会被拒绝。依据：

- 中外产模型分类准确率 **66.63%**（≈随机）
- 模型间可归因度 **68.4%** vs 人机 **93.2%**
- 模型指纹**会被模型更新抹掉**（某模型系列的破折号使用率一年内从「几乎每句」降到几乎不用）

要做特化，请用**篇内自归一化判据**（相对频次），不要用绝对阈值或模型名。

### 3. 用检测器分数当验收标准

依据是数学上的：`TPR ≤ FPR + TV`。
「少误伤人类文字」和「多骗过检测器」是**同一个旋钮的两端**。

而且 12 个检测器里有 6 个在**重度改写**上判定率反而**下降** —— 判定率与 AI 参与度不单调。

### 4. 重新引入已证伪的 18 条

见 `skills/renhua/references/no-touch.zh.md`。包括改短句、删比喻、删问句、
删句内排比、加第一人称、调温度、多轮递归改写等。

如果你有**新的实测数据**能翻转其中某条，欢迎提交 —— 但请带上双侧对照与语料场景。

---

## 开发流程

```bash
git clone https://github.com/RiAnBee/renhua.git
cd renhua
```

改 `skills/renhua/SKILL.md` 或 references 后，**自己先跑一遍负向测试**：

1. 找一段**你自己写的**中文，跑一遍 → 期望**几乎不动**
2. 找一段公文 / 学术摘要，跑一遍 → 期望**零改动**（闸门应生效）
3. 找一段 AI 起草的中文，跑一遍 → 期望改动全部可指到规则号

**改动 ≥3 处的那次，如果输入是人写的，就是误伤。** 请报告。

### 提交前

- `SKILL.md` 保持 **≤350 行**（当前 316 行）
- 新增规则必须同时更新 `references/counterexamples.zh.md`
- 新增证据必须同时更新 `references/evidence.md`，并标证据等级
- 数字必须**可复算**：给出出处、表号或章节号、语料场景

---

## 提 issue

用以下模板能让处理快很多：

```markdown
**类型**：失效报告 / 新证据 / 反例 / 错误更正

**原文**（如果是误伤报告，给出被改坏的原句和 skill 的改法）：

**规则号**：

**你的判断与依据**：

**语料场景**（原生中文 / 翻译 / 不确定）：
```

---

## 协议

贡献默认以 [MIT](LICENSE) 协议授权。
