# 参与贡献

欢迎提供完整语境下的编辑问题：改坏了含义、抹平了口吻、该改却没改，或为了去模板腔换成了另一种模板。不需要先给它套规则号。

## 提交一个可复核的例子

请带上：

- 原稿、实际改稿、用途与读者；必要时提供上下段，避免只截出半句。
- 需要保留的事实、条件、格式或说话分寸。
- 具体损失或改善在哪里，不只说“像AI”“更高级”。
- 编辑模型实际ID、日期、skill版本或内容哈希；来源模型未知就写未知。
- 材料能否公开，以及是否使用合成场景。不要提交私人经历、未授权原文或凭据。

人类稿可以需要编辑，AI稿也可以不改。“改三处就是误伤”已撤回；一处否定被删也可能严重失真，十处措辞调整也可能全部合理。

## 修改核心的要求

`skills/renhua/SKILL.md` 是唯一的运行权限与默认流程。`examples.zh.md` 解释它，不能另立更高优先级；`evidence.md` 是研究摘要，不是每次必读文件。命令与插件描述只指向同一行为，不复制规则。

候选修改应解释：

1. 在什么上下文里存在什么具体问题。
2. 为什么这一编辑动作能改善它，而不是只改变某个统计特征。
3. 同样形式在什么情形下必须保留，尤其是否有语义、语用或作者声音的损失。
4. 相比一个简洁编辑提示和原稿，实测多出了什么价值。

不要求每个候选先证明人机频率差，也不因单次未显著就永久否定。运行时不加检测器、强制台帐、句长配额、厂商规则表或多代理流水线。先删掉没有增益的复杂性，再考虑增加规则。

不要为了压行数去掉限定。提示词的措辞就是行为；边界、示例和默认输出变化都要复测。主文件保持能独立完成普通任务，不能靠强制读另一份长文件来隐藏复杂度。

## 证据与数字

引用论文给出版本、语言、任务、表号或章节，并分开写观察与推断。检测效果、读者偏好和编辑保真不是同一指标。英文研究和英译中结果不能直接变成原生中文规则。

特征探索要检查算子、单位、分母、抽样、语域和长度影响。是否控制句长由问题决定：比较同句长的句法，与描述完整输出是不同问题。多重比较、测量误差、数据污染和同文重复都要说明。

当前有效结论写进证据摘要；过时结论移入历史并标记撤回。不得删除不利结果，也不要把历史小样本比例称为当前真值。

## 验证流程

完整协议见 [evals/README.md](evals/README.md)。

- 修改前冻结旧版；开发前按作者／主题／同源变体隔离确认集。
- 开发比较原稿、简单强提示、旧版、上游与候选。相同输入、用途、模型和生成设置；保护约束不在比较中偷偷变动。
- 先逐文档检查事实、关系、限定、责任、请求力度、来源及结构，再盲评是否更适合用途。数字和代码一致只是辅助证据。
- 成功、失败、空输出与重试分别记录，失败不是零改动。模型实际ID未暴露时说明，不能猜或把别名当跨模型证明。
- 统计单位是完整文档／同源簇，不是调用次数；持出集一旦被用来调提示，就成为开发集。
- 区分静态验证、直接提示、真实skill发现与加载、模型辅助评判、真人盲评。模型评审不能冒充人类。
- 新增长期用例应防范具体可复现风险，不用精确措辞、改动数或规则命中数代替效果。

检查安装目录只包含当前核心及其参考文件；仓库级README、命令、插件、贡献约定与变更日志保持一致。已被迁移淘汰的规则不要留兼容别名或第二入口。

## 许可

贡献按 [MIT](LICENSE) 授权。第三方文章与论文不因进入研究就获得MIT许可；未经授权的全文保存在本地被忽略的目录。引用或复制上游实质内容需遵循其许可证与署名要求。
